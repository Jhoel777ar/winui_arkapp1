<Window
    x:Class="ark_app1.AddCompraDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Title="Registrar Compra">

    <Grid Padding="20" RowDefinitions="Auto, Auto, *, Auto">

        <!-- Header -->
        <TextBlock x:Name="HeaderTextBlock" Grid.Row="0" Text="Registrar Nueva Compra" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,20"/>

        <!-- Provider Selection -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10" Margin="0,0,0,20">
            <ComboBox x:Name="ProveedorComboBox" Header="Proveedor" Width="300" DisplayMemberPath="Nombre" SelectedValuePath="Id"/>
            <Button Content="Agregar Producto" Click="AddProductoButton_Click" VerticalAlignment="Bottom"/>
        </StackPanel>

        <!-- Products List -->
        <controls:DataGrid x:Name="ProductosDataGrid" Grid.Row="2" AutoGenerateColumns="False">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Código" Binding="{Binding Codigo}" IsReadOnly="True"/>
                <controls:DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}"/>
                <controls:DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}"/>
                <controls:DataGridTextColumn Header="P. Compra" Binding="{Binding PrecioCompra}"/>
                <controls:DataGridTextColumn Header="P. Venta" Binding="{Binding PrecioVenta}"/>
                <controls:DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, Mode=OneWay}"/>
                <controls:DataGridTemplateColumn Header="Acciones">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Eliminar" Click="RemoveProduct_Click" Tag="{Binding}"/>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>
            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Vertical" Spacing="10" Margin="0,20,0,0">
            <TextBlock x:Name="TotalTextBlock" Text="Total: 0.00" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Right"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10">
                <Button Content="Cancelar" Click="CancelButton_Click"/>
                <Button Content="Guardar Compra" Style="{StaticResource AccentButtonStyle}" Click="SaveButton_Click"/>
            </StackPanel>
            <InfoBar x:Name="ResultInfoBar" IsOpen="False"/>
        </StackPanel>
    </Grid>
</Window>
