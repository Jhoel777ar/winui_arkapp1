<Page x:Class="ark_app1.AddProductDialogContent"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Visible"
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="20">
        <StackPanel Spacing="16"
                    HorizontalAlignment="Stretch"
                    MaxWidth="700">

            <ComboBox x:Name="ProveedorComboBox"
                      Header="Proveedor (Opcional)"
                      PlaceholderText="Seleccionar proveedor"/>

            <TextBox x:Name="CodigoTextBox" Header="Código * (e.g. PROD001)" PlaceholderText="Ingresa código único"/>
            <TextBox x:Name="NombreTextBox" Header="Nombre * (e.g. Producto Ejemplo)" PlaceholderText="Ingresa nombre"/>
            <ComboBox x:Name="CategoriaComboBox" Header="Categoría (opcional)" PlaceholderText="Selecciona categoría"/>
            <TextBox x:Name="TallaTextBox" Header="Talla (opcional, e.g. Mediana)" PlaceholderText="Ingresa talla"/>
            <TextBox x:Name="ColorTextBox" Header="Color (opcional, e.g. Rojo)" PlaceholderText="Ingresa color"/>
            <TextBox x:Name="UnidadMedidaTextBox" Header="Unidad de Medida" Text="Unidad"/>
            <NumberBox x:Name="PrecioCompraNumberBox" Header="Precio Compra (e.g. 10.50)" Value="0"/>
            <NumberBox x:Name="PrecioVentaNumberBox" Header="Precio Venta * (e.g. 15.00)" Value="0"/>
            <NumberBox x:Name="CantidadNumberBox" Header="Cantidad a Comprar * (e.g. 50)" Value="1" Minimum="1"/>
            <NumberBox x:Name="StockMinimoNumberBox" Header="Stock Mínimo (e.g. 5)" Value="5"/>

            <Button Content="Agregar Producto a la Compra"
                    Click="AddProduct_Click"
                    HorizontalAlignment="Stretch"
                    Background="#0078D7"
                    Foreground="White"
                    FontSize="16"
                    Padding="16,12"
                    CornerRadius="8"
                    Margin="0,10,0,30"/>

            <TextBlock Text="Productos agregados a esta compra:"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,0,0,10"/>

            <ItemsControl x:Name="ProductosItemsControl" Margin="0,10,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Background="#FFF0F0F0" Margin="0,5" Padding="12,10" CornerRadius="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding DisplayText}" VerticalAlignment="Center" FontSize="14"/>
                            <Button Content="Eliminar" 
                                    Tag="{Binding}" 
                                    Click="RemoveProduct_Click"
                                    Grid.Column="1" 
                                    Background="Red" 
                                    Foreground="White" 
                                    Padding="12,6"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</Page>