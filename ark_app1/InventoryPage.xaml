<Page x:Class="ark_app1.InventoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
      mc:Ignorable="d"
      x:Name="root">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <CommandBar Grid.Row="0">
            <AppBarButton Icon="Add" Label="Registrar Compra" Click="RegistrarCompraButton_Click"/>
            <AppBarButton Icon="Edit" Label="Ajustar Stock" Click="AjusteButton_Click"/>
            <AppBarButton Icon="List" Label="Categorías" Click="CategoriesButton_Click"/>
            <AppBarSeparator/>
            <AppBarElementContainer>
                <TextBox x:Name="SearchTextBox" PlaceholderText="Buscar Producto" TextChanged="SearchTextBox_TextChanged" Width="300"/>
            </AppBarElementContainer>
        </CommandBar>
        <Pivot Grid.Row="1" Grid.RowSpan="2">
            <PivotItem Header="Compras (Historial)">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <controls:DataGrid x:Name="ComprasDataGrid"
                                   Grid.Row="0" Margin="10"
                                   AutoGenerateColumns="False"
                                   CanUserSortColumns="True"
                                   CanUserResizeColumns="True"
                                   IsReadOnly="True">
                        <controls:DataGrid.Columns>
                            <controls:DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                            <controls:DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}"/>
                            <controls:DataGridTextColumn Header="Proveedor" Binding="{Binding Proveedor}"/>
                            <controls:DataGridTextColumn Header="Total" Binding="{Binding Total}" />
                            <controls:DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}"/>
                            <controls:DataGridTextColumn Header="Estado" Binding="{Binding Estado}"/>
                            <controls:DataGridTemplateColumn Header="Acciones">
                                <controls:DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <Button Content="Ver Productos" Click="ViewDetails_Click" Tag="{Binding}"/>
                                            <Button Content="Editar" Click="EditCompraButton_Click" Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </controls:DataGridTemplateColumn.CellTemplate>
                            </controls:DataGridTemplateColumn>
                        </controls:DataGrid.Columns>
                    </controls:DataGrid>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10" Margin="10">
                        <Button Content="Anterior" Click="PrevPage_Click"/>
                        <TextBlock x:Name="PageInfoText" Text="Página 1" VerticalAlignment="Center"/>
                        <Button Content="Siguiente >" Click="NextPage_Click"/>
                    </StackPanel>
                </Grid>
            </PivotItem>
            <PivotItem Header="Stock (Productos)">
                <controls:DataGrid x:Name="ProductsDataGrid"
                                   Margin="10"
                                   AutoGenerateColumns="False"
                                   CanUserSortColumns="True"
                                   CanUserResizeColumns="True"
                                   IsReadOnly="True">
                    <controls:DataGrid.Columns>
                        <controls:DataGridTextColumn Header="Código" Binding="{Binding Codigo}"/>
                        <controls:DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" />
                        <controls:DataGridTextColumn Header="Categoría" Binding="{Binding CategoriaNombre}"/>
                        <controls:DataGridTextColumn Header="Precio Venta" Binding="{Binding PrecioVenta}" />
                        <controls:DataGridTextColumn Header="Stock" Binding="{Binding Stock}" />
                        <controls:DataGridTemplateColumn Header="Acciones">
                            <controls:DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Editar" Click="EditProductButton_Click" Tag="{Binding}"/>
                                </DataTemplate>
                            </controls:DataGridTemplateColumn.CellTemplate>
                        </controls:DataGridTemplateColumn>
                    </controls:DataGrid.Columns>
                </controls:DataGrid>
            </PivotItem>
        </Pivot>
        <InfoBar x:Name="InfoBar" Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="10" IsOpen="False"/>
    </Grid>
</Page>